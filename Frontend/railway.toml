# Do NOT use Frontend/ as Railway root directory — shared/ lives in the repo root.
# In Railway: root directory empty, config file deploy/railway-storefront.toml
# Or Dockerfile path: deploy/docker/frontend.Dockerfile
