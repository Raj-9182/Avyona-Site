# Do NOT use Dashboard/ as Railway root directory — shared/ lives in the repo root.
# In Railway: root directory empty, config file deploy/railway-admin.toml
# Or Dockerfile path: deploy/docker/admin.Dockerfile
